repos:
  - repo: local
    hooks:
      - id: io-example-png
        name: generate io-example.png
        entry: go run ./cmd/ioscreenshot --verify io-example.png
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-push]
